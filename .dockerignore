# Keep the Docker build context lean for the dev image.
node_modules
dist
build
.git
.gitignore
.env
.env.local
.env.*.local
.vercel
.idea
*.log
.DS_Store
# Large certificate PDFs are not needed inside the image.
certificates
# Don't include Docker's own files in the context.
Dockerfile
docker-compose.yml
.dockerignore
